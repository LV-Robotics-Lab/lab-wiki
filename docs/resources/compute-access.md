# 算力资源申请与选择

!!! warning "动态资源"
    GPU 数量、在线状态、价格和可用配额会变化。本页只提供选择原则；实际可用性和授权必须在启动任务前向算力管理员实时确认。

## 用途

根据任务是否需要可视化、运行时长、GPU 数量和分布式规模，选择合适的实验室算力资源。

## 前置条件

- 已明确项目、代码仓库、数据位置和负责人。
- 已估算 GPU 类型、显存、卡数、运行时长和存储需求。
- 已在本地或单卡环境完成最小可运行验证。
- 涉及按量计费资源时，已获得项目负责人和算力管理员批准。

## 资源选择

| 任务需求 | 建议资源 | 启动前要求 |
|---|---|---|
| 强可视化、实时交互或远程 GUI | 已授权的实验室工作站 | 先完成个人工作电脑的远程接入申请 |
| 普通训练、单节点临时 GPU | 按量 GPU 平台 | 估算费用；通常不超过单节点 8 卡，以平台当前规格为准 |
| 需要持续运行的单卡任务 | 有限的常开共享服务器 | 申请时间窗口、存储和负责人 |
| 超过单节点规模的分布式训练 | 云端 DLC 等分布式平台 | 先在单卡开发环境完成烟测和一行启动命令 |
| 不需要 GPU | CPU、本地或低成本开发环境 | 不占用稀缺 GPU 资源 |

## 申请信息

- 项目名称和负责人角色
- 代码仓库、目标分支和可复现的环境说明
- GPU 型号、卡数、预计时长和开始时间
- 是否需要桌面可视化、外网、数据挂载或跨节点通信
- 单卡烟测结果、检查点策略和一行启动命令
- 预算来源及任务结束后的关闭责任人

## 操作步骤

1. 按上表选择资源类别，并向 **算力管理员** 提交申请信息。
2. 管理员确认实时可用性、费用、权限和数据路径。
3. 先运行小规模烟测，验证依赖、数据读取、日志和检查点写入。
4. 获得批准后启动正式任务，并记录作业 ID、负责人和预计结束时间。
5. 监控 GPU、日志、费用和检查点；异常时先停止扩容或高成本作业。
6. 作业完成后关闭按量资源，确认数据与检查点已安全落盘。

## 使用规范

- 按量平台必须随用随开、用完即关，禁止无人负责的长期空转。
- 不要把“机器可见”当作“已获授权”；每次使用前重新确认项目分配。
- 大规模训练不能只凭启动成功判断健康，必须检查真实进程、GPU、日志和检查点。
- 禁止在启动命令、环境文件或日志中写入密码、token 和内部地址。
- 需要远程桌面时使用获批渠道，不在 Wiki 保存无人值守访问密码。

## 验证

- 烟测能够完成至少一个有效步骤，并生成预期日志或检查点。
- GPU 数量、进程拓扑和任务配置与申请一致。
- 按量资源在任务结束后已关闭，费用和产物由负责人确认。

## 故障排查

- 无可用 GPU：缩小规模、调整时间或联系管理员重新分配。
- 多卡任务无法通信：保留错误摘要和作业 ID，检查当前平台是否支持目标拓扑。
- 远程桌面不可用：先确认网络路径，再联系远程接入维护者。
- 数据或检查点不可写：停止正式训练，验证权限和剩余空间后再恢复。
- 费用异常：立即停止作业并通知项目负责人和算力管理员。

## 维护信息

- 负责人：算力管理员
- 联系入口：`<CONTROLLED_CONTACT_DIRECTORY_URL>`
- 最后核验：2026-08-03
